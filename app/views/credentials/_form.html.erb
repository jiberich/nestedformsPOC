<%= form_with(model: credential) do |form| %>
  <% if credential.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(credential.errors.count, "error") %> prohibited this credential from being saved:</h2>

      <ul>
        <% credential.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :credential_type, style: "display: block" %>
    <%= form.text_field :credential_type %>
  </div>
  <%= form.fields_for :credential_status do |cs_subform| %>
    <%# fields for the associated address object %>
    <%= cs_subform.text_field :status %>
  <% end %>
  <div>
    <%= form.submit %>
  </div>
<% end %>
